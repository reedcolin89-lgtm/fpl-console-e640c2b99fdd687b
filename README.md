# fpl-console

Read-only status console for the fpl-agent autonomous FPL manager
(personal entry 5560718, "Nostradamus"). Served via GitHub Pages from
this repo's `gh-pages` branch, built and pushed by
[fpl-agent](https://github.com/reedcolin89-lgtm/fpl-agent)'s
`site_build` CI workflow — this repo holds no source, only the built
static site.
